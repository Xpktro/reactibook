import React from 'react';
import { Jumbotron } from 'reactstrap';

export default () => (
  <div>
    <Jumbotron>
      <h1 className="display-3">Reactibook</h1>
      <p className="lead">Or how to use React, Redux and Firebase without failing (much).</p>
    </Jumbotron>
  </div>
);